// tailwind.config.js
/** @type {import('tailwindcss').Config} */
module.exports = {
	content: ["./*.html"], // Adjust as per your file structure
	theme: {
		extend: {}, // Add custom theme overrides if needed
	},
};
